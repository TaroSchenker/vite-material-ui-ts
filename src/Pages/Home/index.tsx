import React from "react";
import { QRCreator } from "../../components";
import { AppContainer, Title } from "../../tw-types";

const Home = () => {
  return (
    <AppContainer>
          <Title> Welcome </Title>
    </AppContainer>
  );
};

export default Home;
